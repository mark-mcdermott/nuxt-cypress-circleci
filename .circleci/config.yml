version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run Nuxt & Run Cypress Tests
          command: docker-compose up --exit-code-from cypress